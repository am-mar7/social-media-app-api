export enum PostType {
  medical = 'medical',
  news = 'news',
  event = 'event',
  other = 'other',
}

export enum PostStatus {
  draft = 'draft',
  published = 'published',
  archived = 'archived',
}
